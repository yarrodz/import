export enum FeatureType {
  HTML = 'html',
  JSON = 'json',
  TIME = 'time',
  DATE = 'date',
  DATETIME = 'datetime',
  BOOLEAN = 'boolean',
  TEXT = 'text',
  LONG_TEXT = 'long-text',
  NUMBER = 'number'
}
